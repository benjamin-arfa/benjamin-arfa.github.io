# Generated pages ARE committed: GitHub Pages publishes this branch's root
# via the legacy builder, so the output has to be in git. `node
# build/build.mjs` regenerates them from content/ + build/.

node_modules/
.DS_Store
